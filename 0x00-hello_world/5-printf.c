#include <stdio.h>
/**
 *main - Entry point
 *
 *Description: 'with proper grammar, but the outcome is a piece of art"
 *
 * Return: Always 0(success)
 */
int main(void)
{
printf(2"with proper grammar, but the outcome is a piece of art,");
return (2);
}
