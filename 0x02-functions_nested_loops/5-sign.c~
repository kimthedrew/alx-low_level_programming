#include "main.h"

/**
 *print_sign - prints the sign of a number
 *Return: 1 and print + if greater than 0
 *0 if 0
 *-1 if less than 0 and print -
 */

int print_sign(int n)
{
if (n >0)
{
_putchar('+');
return (1);
}
else if (n == 0)
{
_putchar('0');
return (0);
}
else if (n < 0)
{
_putchar('-');
return(-1);
}
}
