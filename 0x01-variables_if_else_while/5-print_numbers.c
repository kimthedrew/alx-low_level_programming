#include <stdio.h>
/**
 *main -Entry key to the program
 *
 *Description: prints digits of base 10
 *
 *Return: Always 0(success)
 */
int main(void)
{
char a;
for (a = 0; a < 10; a++)
{
putchar(a);
}
putchar(\n);
return (0);
}
